APP_ENV=testing
APP_DEBUG=true
APP_KEY=your_app_key

# Test Database
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# Cache & session
CACHE_DRIVER=array
QUEUE_CONNECTION=database

# Mail (so no real emails are sent)
MAIL_MAILER=log
